#include <bits/stdc++.h>
using namespace std;


int main() {
    int testcases,n,k;
    cin >> testcases;
    vector<int> arr;
    int temp;
    vector<int> inputs;
    while(testcases--) {
        cin >> n >> k;
        for(int i=0;i<n;i++) {
            cin >> temp;
            inputs.push_back(temp);
        }
    }
}