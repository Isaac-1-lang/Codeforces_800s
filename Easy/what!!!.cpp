#include <bits/stdc++.h>
using namespace std;

int main() {
    int testcases,n,sum;
    cin >> testcases;
    vector<int> results;
    vector<int> possibilities;
    while(testcases--) {
        cin >> n>> sum;
        while (n--)
        {
            int x;
            cin >> x;
            results.push_back(x);
        }
        
}